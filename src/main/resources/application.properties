spring.application.name=tunes
#spring.datasource.url=jdbc:postgresql://localhost:5432/tunes
#spring.datasource.username=postgres
#spring.datasource.password=1550
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.properties.hibernate.format_sql=true

# includes error message in http response
#server.error.include-message=always

spotify.id=${SPOTIFY_ID}
spotify.secret=${SPOTIFY_SECRET}

# global const variable
api.base-url=http://localhost:8080/api/v1

# don't have to speciy path=/api/v1/... in RequestMapping prefix
server.servlet.context-path=/api/v1